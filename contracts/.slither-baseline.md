# Slither baseline — 2026-05-02

Snapshot of high + medium findings against `src/` (excluding `lib/`, `test/`).
Run with:
```
slither src \
    --filter-paths "lib|test" \
    --exclude-dependencies --exclude-informational --exclude-low \
    --solc-remaps "@openzeppelin=lib/openzeppelin-contracts @solady=lib/solady/src forge-std=lib/forge-std/src"
```

## Status: 0 high, 0 medium left after triage

| Detector            | File                            | Severity | Status |
|---------------------|---------------------------------|----------|--------|
| reentrancy-no-eth   | FissionFactory.createMarket     | Medium   | **Fixed** — pushed to `markets` array before the `setTokens` external call (effects-first). |
| locked-ether        | ActionRouter.depositAndSplit    | Medium   | **Fixed** — removed `payable` (we don't consume `msg.value` in v1). |
| locked-ether        | SYBase.receive                  | Medium   | **Fixed** — removed open `receive()`; HBAR sends to SY now revert. ERC-5115 `payable deposit` retained, but reverts on `msg.value > 0` for non-NATIVE tokenIn (no adapter accepts NATIVE in v1). |
| incorrect-equality  | FissionMarket.addLiquidity      | Low/info | **Accepted** — `totalSupply() == 0` is the correct "is pool initialised?" check. |
| uninitialized-local | various median functions        | Low/info | **Accepted** — Solidity zero-initialises memory arrays; the `tmp` array is fully written before reading. |
| unused-return       | various intentional drops       | Low/info | **Accepted** — `(_, _, dec) = sy.assetInfo()` and similar drops are intentional. |

## Production target before mainnet

- 0 high, 0 medium across the whole tree
- Aderyn (Cyfrin) clean run committed alongside Slither baseline
- Mythril deep-dive on `MarketMath` and `FissionMarket._accrueUser`
