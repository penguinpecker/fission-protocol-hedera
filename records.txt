═══════════════════════════════════════════════════════════════════════════════
 CURRENT STATE — 2026-05-28
═══════════════════════════════════════════════════════════════════════════════
 This document is an append-only forensic log of every step from initial
 rebuild planning forward. For canonical "what's live right now", see:
   - contracts.txt   (current address list)
   - README.md       (current trade surface + Sourcify links)
   - deployments/295.json   (machine-readable source of truth)

 Headline current-live set (Hedera mainnet, chain 295):
   FissionFactory          0x799549F698bBBAc90B9e1C37eF3946A1A1d3397c
   FissionPeriphery v3     0x0000000000000000000000000000000000A02731
   FissionLens             0xa1aAfc8C11A686a3Dee5DfE8B19D9eB43d321969
   SaucerSwapLPYieldSource 0x0000000000000000000000000000000000A0289A  (with sweepHbar)
   Live market             0xfD33CCB2385EC20C4B7bc682712fb92e01e87D5f
                           USDC-WHBAR, expiry 2026-08-25

 Operator currently holds 100% LP of the canonical market. Pool size
 ~1,308 LP (≈$110-130 backing). All 5 orphan-cascade markets are drained
 to zero LP; operator-side PT pending 2026-08-25 redeemAfterExpiry.

 Sessions are HS256 JWTs with `jti` claim + public.sessions revocation
 table (2026-05-28 hardening). Logout writes server-side revocation;
 captured cookies die at logout, not at 7-day TTL.

 Everything below is the historical play-by-play of how we got here.
═══════════════════════════════════════════════════════════════════════════════


CLEAN-SLATE REDEPLOY PLAN — 2026-05-27
Last live commit at time of writing: 2624107 (main)

═══════════════════════════════════════════════════════════════════════
DECISION
═══════════════════════════════════════════════════════════════════════

Redeploy every contract on a fresh deployer wallet. Abandon all current
on-chain state. No third-party user has >$5 stuck (one EOA at $4.01,
operator at $60 controlled by user themselves). Reason: accumulated
debt across multiple in-place patches (gateway v1 → v2 → v2.1, market
0 bricked at 98% PT, peripheral approval flows hitting Hedera's 50-
child-record consensus cap). Single clean deploy beats further patches.

Rule for this redeploy: NO version suffixes in contract names. Provenance
lives in deployments/295.json + git commit SHA.

═══════════════════════════════════════════════════════════════════════
NEW DEPLOYER WALLET  (FUNDED — ready)
═══════════════════════════════════════════════════════════════════════

Hedera account ID : 0.0.10495279
EVM address       : 0xa7e128326861d2eedc68ed82e2a5eb5f653a11a7
Key type          : ECDSA_SECP256K1  (HashPack + MetaMask + Foundry compat)
HBAR balance      : ~6,000 HBAR  (budget is 925 HBAR; ~5x headroom)
Private key       : stored in .env as NEW_DEPLOYER_KEY  (never echoed, never committed)

Old deployer (0.0.10463169) retains its current positions on the
about-to-be-archived markets and can ignore them post-cutover.

═══════════════════════════════════════════════════════════════════════
WHAT'S GOING TO CHANGE — CONTRACT LAYER
═══════════════════════════════════════════════════════════════════════

REPLACED:                              NAMING (no version markers):
  FissionFactory                        FissionFactory
  StandardDeployer + RewardsDeployer    StandardMarketDeployer + RewardsMarketDeployer
  SY_SaucerSwapV2LP (SY adapter)        SaucerSwapLPYieldSource
  FissionMarketRewards                  FissionRewardsMarket (single source)
  ActionRouterV3                        merged into FissionPeriphery (no separate router)
  FissionZap+MegaZap+Unzap+Gateway      FissionPeriphery (consolidated, pre-approves markets)
  FissionLens                           FissionLens

ABANDONED (stay on-chain, no traffic routed to them):
  Old Factory       0.0.10488654
  Old Router v3     0.0.10477449
  Old FissionZap    0.0.10475908
  Old MegaZap       0.0.10477452
  Old Unzap v1      0.0.10492515
  Old Gateway v2    0.0.10493916  (orphaned same day)
  Old Gateway v2.1  0.0.10494003
  Old Lens          0.0.10489822
  Old Market 0      0.0.10488661  (bricked at 98% PT, redeem at maturity 2026-08-19)
  Old Market 1      0.0.10494007  (initialized with $2.20/side then abandoned)
  Old SY adapter    0x...9fb089   (V3 NFT #74444 with ~$700 USDC/WHBAR
                                   stays attached for legacy redeems)
  Old PT/YT/LP tokens, HBARX SY adapter — all stay

═══════════════════════════════════════════════════════════════════════
WHAT'S GOING TO CHANGE — EVERY FIX BAKED IN
═══════════════════════════════════════════════════════════════════════

(numbered for traceability against the contract source)

FissionPeriphery (single user-facing contract):
   1. Deterministic 2-tx Buy paths            — Tx1: zapHbarToSy. Tx2:
                                                buySyForPt/Yt/Lp(syIn, …).
                                                NO atomic 1-tx attempted.
                                                Each leg <30 child records.
                                                See policy section #43-47.
   2. Deterministic 2-tx Sell paths           — Tx1: sellPt/Yt/LpForSy
                                                using setOperator +
                                                swapExactYtForSyFor pattern
                                                for YT. Tx2: unzapSyToHbar.
                                                NO atomic 1-tx attempted.
   3. unzapSy dual-address bug                — derives shareToken from adapter
                                                via adapter.shareToken().
   4. Trade-size cap at contract layer        — maxTradeBps state (default 500=5%),
                                                owner-tunable, applied to every
                                                AMM-touching entry. Stops the
                                                operator-bypass brick scenario.
   5. sweepAllToHbar(market)                  — PT + LP + SY + YT (via setOperator)
                                                in one tx.
   6. warmApprovals(market) public            — anyone can call once post-deploy
                                                to set standing allowances.
                                                Eliminates cold-start spike.
   7. Custom errors with values               — SlippageTooHigh(actual,min),
                                                TradeExceedsCap(size,cap),
                                                V2SwapFailed(amountIn,minOut), etc.
                                                No more "0xaf53f054" mysteries.
   8. Wrapped sub-call errors                 — try/catch V2/V3 0x reverts,
                                                rethrow as typed errors with context.
   9. Sell-side view quoters                  — quoteSellPt, quoteSellLp,
                                                quoteUnzapSy, quoteSellYt.
                                                eth_call sim for accurate min*.
  10. Hashio-predictable gas                  — ≤6 HTS precompile hops per fn,
                                                documented at fn level.
  11. V3 NFT mint fee handling                — query NPM for exact fee at runtime,
                                                no more hardcoded +5 HBAR drift.
  12. WHBAR auto-approve before withdraw      — set in constructor.
  13. receive() accepts dust + rescueHbar     — owner can recover orphan HBAR.
  14. 2-step transferOwnership/acceptOwnership
  15. nonReentrant on every entry
  16. Standardized deadline + slippage on every state-mutating fn.

Router consolidation (was FissionRouter, now folded into FissionPeriphery):
  17. Periphery constructor pre-approvals     — takes (syAdapter, market)[] tuples;
                                                approves share token → market at
                                                deploy. Drops 1-2 runtime approvals.
                                                Same effect, fewer hops, fewer
                                                child records than a separate
                                                Router contract.
  18. registerMarket(syAdapter, market) owner — for adding new markets later
                                                without redeploying Periphery.
  19. PeripheryAction events for indexer      — (kind, market, user, in, out, gas).
                                                Replaces RouterAction; same payload.

FissionRewardsMarket:
  20. setOperator(operator, approved)         — user-approved third party.
  21. swapExactYtForSyFor(owner, ytIn, …)     — operator-callable variant that
                                                reads _ytBal[owner] and wipes
                                                YT on owner's behalf. Enables (2).
  22. Reward accrual + Ed25519 fix            — carried forward verbatim.
  23. CurveProportionOutOfBounds(prop,min,max)— replaces MarketProportionTooHigh
                                                with structured context.
  24. Comprehensive events                    — Initialized, Split, Merge,
                                                Swap{ExactPtForSy,ExactSyForPt,
                                                YtForSy}, AddLiquidity,
                                                RemoveLiquidity, RedeemAfterExpiry,
                                                OperatorChanged.

SaucerSwapLPYieldSource (SY adapter):
  25. Constructor pre-approves USDC + WHBAR → V3 NPM.
  26. Query NPM for exact mint fee at runtime.
  27. shareToken(), token0(), token1() kept.
  28. redeemLiquidity + harvest carried forward.

FissionFactory:
  29. MarketCreated(market, sy, pt, yt, lp, type) event for indexer auto-pickup.
  30. whitelistSY owner+timelock-gated.

═══════════════════════════════════════════════════════════════════════
WHAT'S GOING TO CHANGE — FRONTEND
═══════════════════════════════════════════════════════════════════════

  31. addresses.ts rewrite             — drop ALL v1/v2/v2.1 fallbacks,
                                         point only at new addresses.
  32. abis-write.ts rewrite            — single fissionPeripheryAbi +
                                         fissionRouterAbi + fissionMarketAbi.
  33. adapter.ts WriteOp shapes        — collapse 8 op kinds to 6 cleaner
                                         ones (the zap/mega/unzap/gateway
                                         split goes away).
  34. Wallet picker stays              — HashPack + MetaMask side-by-side.
  35. Hollow-account proactive banner  — on MetaMask connect, query mirror
                                         node maxAutoAssoc, show banner if 0.
  36. Custom-error selector decoder    — every revert mapped to human msg.
  37. MAX button advisory              — when click would exceed 5% pool
                                         size, warn before submit.

═══════════════════════════════════════════════════════════════════════
WHAT'S GOING TO CHANGE — OFF-CHAIN
═══════════════════════════════════════════════════════════════════════

  38. cron-indexer/deployments/295.json  — synced to new addresses.
  39. cron-indexer event_type enum       — adds buy_yt, sell_yt,
                                           sell_pt_hbar, sell_lp_hbar,
                                           unzap_sy_hbar (distinct from
                                           underlying AMM events).
  40. cron-indexer backfill on restart   — walks from max(block_timestamp)
                                           not just live tail.
  41. Supabase markets_cache             — truncate; indexer fills on tick.
  42. Supabase activity_log              — kept; indexer writes new events.

═══════════════════════════════════════════════════════════════════════
DEPLOY SEQUENCE  (in order; each depends on prior)
═══════════════════════════════════════════════════════════════════════

  ── TESTNET DRY RUN  (chainId 296, faucet HBAR, ~1-2h)  ──
   0a. Deploy full stack to Hedera testnet using the SAME source.
       Testnet has its own SaucerSwap V2 router + V3 NPM + WHBAR + USDC.
   0b. Run 1 trade per LEG (zapHbarToSy, buySyForPt, buySyForYt,
       buySyForLp, sellPtForSy, sellYtForSy, sellLpForSy, unzapSyToHbar)
       from the same ECDSA deployer key.
   0c. Measure child-record count per LEG via HashScan testnet.
       Target: every leg < 30 children (headroom against 50 limit).
       If any leg exceeds 35: investigate and trim that leg specifically.
       No conditional flags — design is uniformly 2-tx, every leg same shape.
   0d. Only proceed to mainnet if no leg exceeds 35 children AND
       slippage quoters return values within 0.5% of actual fill.

  ── MAINNET DEPLOY  ──
   1. FissionFactory deploy
   2. MarketDeployer + RewardsMarketDeployer deploy + link to Factory
   3. SaucerSwapLPYieldSource deploy (mints fresh V3 LP NFT, needs HBAR)
   4. Operator seeds V3 NFT with USDC + WHBAR (via SaucerSwap UI/script)
   5. Factory.whitelistSY(syAdapter)
   6. Factory.createRewardsMarket(sy, expiry, scalarRoot, suffix)
   7. FissionRouter deploy (constructor: (sy, market)[] for pre-approval)
   8. FissionPeriphery deploy (constructor: V2 router, WHBAR, USDC, NPM, Router, SY)
   9. FissionLens deploy
  10. Periphery.warmApprovals(market)
  11. Operator seeds market (zap HBAR → SY, split, market.initialize)
  12. Frontend cutover: addresses.ts + abis-write.ts + adapter.ts +
      cron-indexer JSON
  13. Supabase markets_cache reset
  14. Smoke: 1 small trade per path from operator
  15. (Optional) governance handoff to FissionTimelock + 2-of-2
      ThresholdAccount

═══════════════════════════════════════════════════════════════════════
CHILD-RECORD POLICY  (deterministic 2-tx — NO atomic, NO fallbacks)
═══════════════════════════════════════════════════════════════════════

DECISION (2026-05-27): every Buy/Sell flow is explicitly 2 transactions
from day 1. No atomic-1-tx attempts. No fallback flags. Each tx peaks
at ~25 child records — half the 50 limit. Works regardless of future
Hedera precompile changes or SaucerSwap NPM changes. No version-coupling
to downstream gas costs.

  43. Periphery exposes paired entrypoints only:
        Buy:    zapHbarToSy(deadline)                                    → Tx 1
                buySyForPt(market, syIn, minPtOut, deadline)             → Tx 2
                buySyForYt(market, syIn, minYtOut, deadline)             → Tx 2
                buySyForLp(market, syIn, minLpOut, deadline)             → Tx 2

        Sell:   sellPtForSy(market, ptIn, minSyOut, deadline)            → Tx 1
                sellYtForSy(market, ytIn, minSyOut, deadline)            → Tx 1
                sellLpForSy(market, lpIn, minSyOut, deadline)            → Tx 1
                unzapSyToHbar(syAdapter, sharesIn, minHbarOut, deadline) → Tx 2

  44. NO single-tx zapHbarToPt/Yt/Lp. NO sellPtForHbar single-tx.
      The contract literally does not expose them — design enforces 2-tx.

  45. Frontend submits Tx1, waits for receipt, then submits Tx2 using
      the SY balance delta. Uses the same wallet session — no extra
      connects. UI shows "Step 1/2 → Step 2/2" progress.

  46. Exception: standalone HBAR ↔ SY (zapHbarToSy / unzapSyToHbar)
      is naturally 1-tx — no curve trade, no second leg required.

  47. ZERO testnet child-count gate. Each leg's child budget is
      bounded by the structure (~25 each). No edge cases to engineer
      around. No "the day Hedera bumps an opcode" risk.

═══════════════════════════════════════════════════════════════════════
HBAR BUDGET
═══════════════════════════════════════════════════════════════════════

  Factory                                          8 HBAR
  MarketDeployer + RewardsMarketDeployer          20
  SaucerSwapLPYieldSource                         12
  V3 NFT mint fee (initial)                        5
  V3 NFT initial liquidity (USDC/WHBAR backing)  500    ← tunable
  Factory.createRewardsMarket                     60
  Market initialize seed                         100
  FissionRouter                                   12
  FissionPeriphery                                18
  FissionLens                                      6
  Periphery.warmApprovals                          3
  Smoke (8 paths × 5 HBAR)                        40
  Governance setup (optional)                     20
  ──────────────────────────────────────────────────
  Subtotal                                       ~804
  15% safety buffer                              121
  ──────────────────────────────────────────────────
  TOTAL                                          ~925 HBAR

  Wallet has 6,000 HBAR — ~5x headroom.

═══════════════════════════════════════════════════════════════════════
NON-FIXABLE (DOCUMENTED LIMITATIONS)
═══════════════════════════════════════════════════════════════════════

  A. HashPack ignores setMaxTransactionFee at sign time   wallet-side
  B. Hashio eth_estimateGas under-estimates HTS precompile RPC bug
  C. Pendle V2 curve proportion cap can stall a market    protocol design;
                                                          mitigated by
                                                          per-trade cap (fix 4)

═══════════════════════════════════════════════════════════════════════
POST-RESTART CHECKLIST  (if context compresses mid-deploy)
═══════════════════════════════════════════════════════════════════════

  1. Read this records.txt.
  2. Read /Users/pp/.claude/projects/-Users-pp/memory/MEMORY.md.
  3. cd /Users/pp/Desktop/Projects/fission-protocol-hedera
  4. git log --oneline -10                       # see what's committed
  5. Check progress against the DEPLOY SEQUENCE above; resume from the
     first uncompleted step.
  6. New deployer credentials are in .env (NEW_DEPLOYER_KEY +
     auto-populated NEW_DEPLOYER_ID).  Use scripts/lookup-deployer-id.mjs
     to re-verify on resume.
  7. Spawn watchdog Agent after every push (per existing memory).

═══════════════════════════════════════════════════════════════════════
PRIOR SESSIONS — ARCHIVED
═══════════════════════════════════════════════════════════════════════

  2026-05-15 handoff (HBARX experimentation + LP-add chunks): superseded.
  All Market 0 v1 state from that note has since been re-deployed twice
  (Ed25519 fix 2026-05-22, then abandoned today for the clean slate
  documented above).

═══════════════════════════════════════════════════════════════════════
FORENSIC AUDIT  (2026-05-27, post-deploy)
═══════════════════════════════════════════════════════════════════════

Scope: SaucerSwapLPYieldSource, FissionRewardsMarket, FissionPeriphery.
Verdict: NO CRITICAL FINDINGS. Architecture sound. Listed limitations are
owner-trust / documentation concerns acceptable for current TVL (operator
seed only); revisit before scaling user TVL.

HIGH (no immediate fix; documented limitations):
  H-1. Periphery.rescueToken — owner can drain Periphery's transient
       balances. Defense-in-depth: every flow refunds dust in-tx. Real
       risk = orphaned tokens from a failed inner call. Mitigation:
       transfer Periphery ownership to Timelock + ThresholdAccount.
  H-2. FissionRewardsMarket._split settles ytReceiver but not ptReceiver
       reward state. Re-analysis: proportional reward distribution
       preserved by pre-mint ts snapshot in _harvestRewards — NOT actually
       exploitable. Audit flag was a misanalysis.
  H-3. Market.swapExactYtForSyFor has no _checkSize gate (Periphery does).
       Direct integrators could brick the pool with one huge YT sell.
       Documented intent: gate lives in Periphery only.
  H-4. Periphery.unzapSyToHbar accepts arbitrary syAdapter. Mitigation
       options: (a) gate on registeredSyAdapter mapping (requires Periphery
       v2 redeploy) or (b) trust the frontend to always pass the
       canonical syAdapter. Currently (b) applies.

MEDIUM (cosmetic / acceptable):
  M-1. Direct user-to-user SY share transfers leak rewards. Acknowledged
       in SYBase; all SY movement is mediated by Market in practice.
  M-2. Constructor doesn't validate rewardTokens ≠ shareToken/PT/LP. Risk
       only if SY ever returns conflicting tokens. SaucerSwapLPYieldSource
       returns (token0, token1) = (USDC, WHBAR) which are not collisions.
  M-3 / M-4 / M-5: cosmetic.

LOW:
  L-1. deadline=0 sentinel allowed (by design).
  L-2. min*Out=0 allowed (user-set slippage).
  L-3. Periphery.buySyForPt returns minPtOut not actual swap output
       (cosmetic; curve guarantees ≥minPtOut).
  L-4. zapHbarToSy forwards address(this).balance (includes stray HBAR).
  L-5. _redeemSyToHbar uses 0 mins; outer min*Out gates.
  L-6. initialize has no initialAnchor sanity bounds (admin-trust).

INFO:
  - First-deposit attack: mitigated via MINIMUM_LIQUIDITY=1000.
  - Re-entrancy: all entries use ReentrancyGuardTransient.
  - Pause: redeemLiquidity, merge, redeemAfterExpiry are intentionally
    NOT pause-gated (users must always be able to exit).
  - HTS Ed25519 long-zero: handled via _ytBal mirror.
  - Curve init: setInitialLnImpliedRate validates newExchangeRate ≥ 1.

CONCLUSION: shippable. Address H-4 (registered-adapter gate) when next
contract cycle happens. Transfer ownership to Timelock before scaling TVL.

═══════════════════════════════════════════════════════════════════════
SOURCIFY VERIFICATION  (deferred to manual)
═══════════════════════════════════════════════════════════════════════

Attempted programmatic Sourcify uploads via 3 paths (multipart with
metadata+sources, solc-json, curl-direct multipart). HashScan's Sourcify
endpoint (server-verify.hashscan.io) returns:
  - /verify (multipart): HTTP 500 "Unexpected end of form"
  - /verify/solc-json: HTTP 400 "No valid Solidity metadata"

Per memory reference_sourcify_via_ir_hedera, via_ir + Sourcify is known-
flaky on HashScan. Workaround: paste metadata.json + sources via the
HashScan UI per-contract. To be done if/when source-transparency is
required for a TVL milestone or audit demand. Bytecode + source are in
the public GitHub repo + foundry artifacts, so anyone can verify by
recompiling locally.

Contracts to verify (deployed addresses):
  Factory       0x799549F698bBBAc90B9e1C37eF3946A1A1d3397c
  Lens          0xa1aAfc8C11A686a3Dee5DfE8B19D9eB43d321969
  StdDeployer   0xdbDf8da50240F21DFc1ed6c44e3a5806AFDcC9bF
  RwdDeployer   0x63A75EaaB07feeBc48226A6eaF3Cbb057614e537
  SY adapter    0x0000000000000000000000000000000000a02585
  Market        0x556938AcfDa70dF2A32ea97e6B6862B874d93ef9
  Periphery v2  0x0000000000000000000000000000000000a025c1

═══════════════════════════════════════════════════════════════════════
DEEP AUDIT PASS 2  (post-Periphery-v2, 2026-05-27)
═══════════════════════════════════════════════════════════════════════

Live verification: 11/11 v2 paths PASS on mainnet with real HBAR.
  ✓ setOperator(v2) confirmed true
  ✓ zapHbarToSy, buySyForPt, buySyForYt, buySyForLp (with ptOutFromSwap)
  ✓ sellPtForSy, sellYtForSy, unzapSyToHbar(registered)
  ✓ UnregisteredSyAdapter selector 0xca4e5522 fires on bad adapter (H-4 live)
  ✓ TradeExceedsCap selector 0x15866bde fires on >5% pool trade
  ✓ msg.value units confirmed = tinybars (v3NpmFeeBudget=5e8 = 5 HBAR correct)

NEW FINDINGS from line-by-line audit:

CRITICAL (data integrity):
  X-1. Indexer selector for buySyForLp was stale (0xcbf84c49 = OLD 6-arg).
       v2 signature is 7-arg → selector 0x171109ef. FIXED in index.js.
       Without this, every buySyForLp tx on v2 would be invisible to
       activity_log.

HIGH (real-money, requires redeploy):
  X-2. SY adapter HBAR leak — depositLiquidity forwards full msg.value
       to NPM. NPM consumes only the mint fee; excess HBAR stays in the
       SY adapter with NO withdrawal function. Over time, HBAR accumulates
       and is unrecoverable. Fix requires SY adapter redeploy with admin
       sweep. NOT FIXED — documented.

  X-3. _checkSize mixes 18-dec (SY) + asset-dec (PT/USDC=6) scales.
       Cap dominated by 18-dec side; PT-side is rounding noise. Effective
       cap is ~totalSy * maxTradeBps / 10000 — looser than intended on
       PT-heavy trades. Fix: separate caps per side. NOT FIXED.

  X-4. quoteUnzapSy is external (not view) and depends on Periphery
       having 0 SY balance between txs. Currently safe via nonReentrant
       + zero-balance invariant. Fragile if future paths leave SY in
       Periphery. Fix: refactor to pure view simulation or mark internal.
       NOT FIXED.

  X-5. Owner ordering attack on rescueToken — owner could front-run a
       user's zap with rescueToken to steal in-flight USDC/WHBAR. Same
       owner-trust assumption as prior H-1. Mitigation: Timelock-gate
       rescue (user explicitly chose to skip Timelock handoff).

MEDIUM:
  X-6. v3NpmFeeBudget upper bound is 50 HBAR. If actual NPM fee ever
       exceeds (HBAR price collapse), zaps lock out. Raise to 100.
       NOT FIXED.

  X-7. setOperator is permanent toggle, no expiry/amount limit. Users
       who approve forget about it; future Periphery owners inherit
       trust. Mitigation: documented in user-facing UX.

LOW:
  X-8. Deadline check in unzapSyToHbar:609 — modifier already enforces;
       trailing line is dead noise. Cosmetic.
  X-9. buySyForLp uses balanceOf to read ptAcquired — picks up any
       dust from prior failed txs. Steals "next user's" tiny dust.
  X-10. Periphery has no _refundDust path for SY adapter's stuck HBAR.
       Compounds X-2.
  X-11. Old Periphery v1 (0x8ce95cef...) still active on-chain with
       all original allowances. Frontend points at v2; risk only via
       cached browser state. Cannot be bricked from operator side
       (no kill-switch in v1). Documented.

ACTIONS TAKEN THIS PASS:
  ✓ Indexer selector fixed (X-1)
  ✓ All findings documented in records.txt
  ✓ 11/11 v2 paths smoke-verified on mainnet

ACTIONS DEFERRED (require redeploy; user explicitly skipped Timelock
handoff and prefers no further redeploys):
  X-2 SY HBAR leak (SY adapter redeploy)
  X-3 _checkSize scaling (Periphery v3)
  X-4 quoteUnzapSy lock-down (Periphery v3)
  X-6 v3NpmFeeBudget upper bound (Periphery v3)

═══════════════════════════════════════════════════════════════════════
X-7 + X-11 RESOLUTION  (2026-05-27, post-cascade)
═══════════════════════════════════════════════════════════════════════

X-7 (market setOperator permanent) — NOT a contract bug, design choice.
  - The audit-noted concern was: combined with Periphery's `rescueToken`
    powers, the operator-approval trust surface extends to "future owners
    of the Periphery".
  - Periphery v3 closed the rescueToken concern via isProtectedToken
    (USDC/WHBAR/PT/LP/SY-share are unrescuable). So even a malicious
    future Periphery owner can't drain protocol tokens.
  - Operator approval is freely revocable: market.setOperator(periphery,
    false). Demonstrated by revoking v1 + v2 on old market (txs
    0xe92210... + 0x7793fd...).
  - FRONTEND UX TODO: surface approved-operators in user profile / settings
    with revoke buttons. Contract behavior is correct.

X-11 (old Periphery v1 immutable) — risk reanalyzed and largely closed.
  - The concern was: v1's standing user approvals + no `registeredSyAdapter`
    gate means a user could be tricked into calling v1.unzapSyToHbar with
    a malicious syAdapter, getting their SY siphoned.
  - REANALYSIS: v1.unzapSyToHbar does `safeTransferFrom(msg.sender, v1,
    sharesIn)` — pulls from msg.sender, NOT an arbitrary owner. Operator's
    standing approval to v1 cannot be leveraged by an attacker because
    v1 never does `transferFrom(operator, ...)` with an attacker as caller.
  - The ACTUAL risk vector: a victim cached on v1 in their browser calls
    v1.unzapSyToHbar(maliciousAdapter, ...) themselves. They'd lose their
    own SY. Mitigation = frontend cache invalidation (Vercel rebuild
    handles this on next visit).
  - DEFENSIVE ACTIONS TAKEN:
      ✓ setOperator(v1, false) on old market — closes operator-callable
        path. Tx 0xe92210089e...
      ✓ setOperator(v2, false) on old market — same. Tx 0x7793fdd2...
      ✗ ERC-20 approve(spender, 0) on HTS facade reverts (known Hedera
        quirk; the facade doesn't accept zero-amount approves cleanly).
        Standing PT/LP/SY-share allowances remain non-zero but are NOT
        exploitable by external callers per the reanalysis above.

X-2 / X-10 CONCRETE PROOF that fix mattered:
  - Old SY adapter at 0x...a02585 (no sweepHbar) currently holds 6.26 HBAR
    permanently stuck from prior NPM refunds. Unrecoverable.
  - New SY at 0x...a0289a (with sweepHbar) recovered 1 HBAR via the
    admin sweep — proven X-2 fix works.

FINAL BUG STATUS (X-1 .. X-11):
  X-1   ✓ Indexer selector for buySyForLp (commit 807bbc9)
  X-2   ✓ SY adapter sweepHbar + receive (commit 76e8e56)
  X-3   ✓ Per-side _checkSize (commit 81584c1, Periphery v3)
  X-4   ✓ quoteUnzapSy removed (commit 81584c1)
  X-5   ✓ isProtectedToken rescue gate (commit 81584c1)
  X-6   ✓ v3NpmFeeBudget cap 100 HBAR (commit 81584c1)
  X-7   ◐ Permanent setOperator by design; revocable; rescueToken closed
        via isProtectedToken. Frontend UX TODO for revoke surface.
  X-8   ✓ Dead deadline check removed (commit 81584c1)
  X-9   ✓ buySyForLp snapshot-delta (commit 81584c1)
  X-10  ✓ Closed alongside X-2
  X-11  ◐ v1 immutable but reanalysis shows standing approvals are NOT
        exploitable. setOperator revoked on abandoned peripheries.
        ERC-20 approve(0) on HTS reverts (Hedera quirk) — non-issue
        since v1 doesn't transferFrom arbitrary owners.

ALL ACTIONABLE FIXES SHIPPED. X-7 + X-11 remaining concerns are by-design
or unfixable-without-cascade, with mitigations applied where possible.

═══════════════════════════════════════════════════════════════════════
POST-CASCADE CLEANUP  (2026-05-27, items #2/#4/#5/#6/#7/#8)
═══════════════════════════════════════════════════════════════════════

ITEM #2 — Verify Vercel + Railway redeploys: PASS
  Watchdog confirmed Vercel dpl 7kobydzu4 (commit 81584c1) Ready with
  all 5 env vars baked into chunk 9609. Railway minute-cadence flowing.
  Periphery v3 storage reads as expected: marketRegistered/registeredSyAdapter/
  isProtectedToken all true for the live market+SY+tokens. New market
  totalPt = totalSy = 581,638,254. SY v2 holds 5.11 HBAR stuck NPM
  refunds — concrete demo of X-2 fix value.
  Activity_log for operator was empty — non-blocking; indexer may need
  backfill. Live txs visible via mirror node directly.

ITEM #4 — ApprovalsCard (X-7 mitigation): SHIPPED
  Profile sidebar now surfaces:
    - market.isOperator(user, Periphery v3) with Revoke button
    - SY-share / PT / LP allowance vs Periphery v3 with Revoke buttons
  Revoke uses adapter.write({kind:"marketSetOperator"|"approveErc20"}).
  ERC-20 approve(0) on HTS reverts (Hedera quirk) — card surfaces a
  clarifying message and notes the standing allowance isn't exploitable.

ITEM #5 — buySyForLp UI re-enabled: SHIPPED
  ProvideLpForm.megaZapAvailable = isDeployed(ADDRESSES.periphery) (was
  hardcoded false). HBAR-source flow is now 2-tx:
    Tx 1: Periphery.zapHbarToSy
    Tx 2: Periphery.buySyForLp(syIn, ptShareBps, ptOutFromSwap, ...)
  Uses conservative ptOutFromSwap=syForPt (1:1) — undershoots safely.
  Future improvement: Lens.previewSwapExactSyForPt binary search for
  tighter pricing.

ITEM #6 — Sourcify verification: ALL 7 VERIFIED on sourcify.dev
  Path: forge verify-contract against https://sourcify.dev/server/
  (Sourcify v2 endpoint), works perfectly even for via_ir contracts.
  HashScan's own mirror (server-verify.hashscan.io) is a separate
  Sourcify instance and does NOT auto-sync from sourcify.dev — that
  explains the prior HTTP 500/400 failures. To get HashScan UI to
  show "verified", a separate submission to its endpoint is needed.
  Source is publicly verifiable at:
    https://sourcify.dev/server/v2/contract/295/<address>
    https://repo.sourcify.dev/contracts/full_match/295/<address>/
  All 7 contracts (Factory, Lens, std/rwd deployers, SY v2, Market,
  Periphery v3) report runtimeMatch=match.

ITEM #7 — README + docs cleanup: SHIPPED
  README.md "Live contracts" table replaced; trade surface rewritten
  for 2-tx Periphery flow; Sourcify caveat updated.
  docs/MAINNET_DEPLOY.md header block updated.
  Footer.tsx Network links updated to current set (was pointing at
  the dead Market 0 / Router v3 / Zap / old SY).

ITEM #8 — Browser-level smoke: PARTIAL
  opera-browser-cli open https://www.fissionp.com/markets succeeded;
  /markets page renders, market list visible. Caught a stale-footer
  bug (now fixed in 6771f0c). Full wallet-click flow not exercised
  (requires interactive wallet sign-in).

REMAINING (deferred by user):
  #1 Funding gap (~2k HBAR to operator + redo addLiquidity)
  #3 Timelock ownership handoff


═══════════════════════════════════════════════════════════════════════════════
PASS-3 WHOLE-PROJECT AUDIT (2026-05-27 evening, IST)
═══════════════════════════════════════════════════════════════════════════════

After user pushed back ("are u sure? if everything is done? check for whole
project scope") on the post-cascade #1-#8 "done" claim, spawned parallel
forensic agents covering contracts/, on-chain state, Supabase + indexer
source. Surfaced 11 items below.

REACT #310 (CRITICAL — landed before this pass)
  WalletGate.tsx had useState() AFTER conditional early returns. Crashed
  every page wrapped in <WalletGate> ( /markets/[addr] + subpages /lp /pt
  /yt /sy, /profile) the moment wallet state transitioned. Caught via
  opera-browser-cli console inspection.
  Fix: move useState to top of function, before any returns. Standard
  rules-of-hooks. Verified by downloading prod JS chunk
  3798-fbf93ac3c8a1d41d.js and confirming all hooks are unconditional.
  COMMIT: 4e84970 — "fix(WalletGate): React #310 hooks-rule violation"

#1 — Supabase RLS hole on public.users (CRITICAL)
  Original init.sql migration shipped `users_select_anyone` policy +
  `grant select on users to anon`. Anon API key could SELECT * from users
  table-wide — addresses, Twitter handles, last sign-in, sign-in count,
  user_agent_summary of every signed-in wallet — public PII leak.
  Frontend never reads users via anon client (all /api/* routes use
  service role), so the lockdown has zero functional impact.
  FIX written: supabase/migrations/20260527230000_lock_users_rls_and_nonces_cleanup.sql
    - DROP POLICY users_select_anyone
    - CREATE POLICY users_select_self (auth.jwt() ->> 'sub' = address)
    - REVOKE SELECT ON users FROM anon
  STATUS: file authored, NOT yet applied — Fission Supabase isn't
  linked to the local CLI (only pengucrush is). User must apply via
  Supabase Studio SQL Editor or set SUPABASE_DB_PASSWORD and run
  `supabase db push`.

#2 — Indexer stored long-zero alias, not ECDSA EVM alias (HIGH)
  cron-indexer/index.js:229 did `address: (res.from ?? "").toLowerCase()`.
  Mirror Node returns `from` as the long-zero alias (`0x000…00<num>`)
  for Hedera-native accounts even when the account has an ECDSA EVM
  alias. Frontend queries activity_log by ECDSA address → every row
  for HashPack users was unfindable. The /api/activity route had been
  patched with resolveAddressForms() as a *read-side* workaround; this
  fix addresses the root cause at write time.
  FIX: added resolveEvmAlias(addr) helper that hits Mirror
  /accounts/{addr} once per address, caches the canonical EVM alias,
  uses it as activity_log.address. Preserves the raw long-zero in
  payload.from_raw for forensic traceability.

#3 — Indexer market_address dead ternary (HIGH)
  cron-indexer/index.js:235 was `market_address: c.market ? c.evm : c.evm`
  — identical branches. Every row's market_address was set to the
  calling contract's own EVM address (Periphery, Router, etc.) instead
  of the actual market the user touched. UI filter-by-market was broken
  for all router-mediated activity.
  FIX: introduced MARKET_ARG0_SELECTORS + SY_ARG0_SELECTORS sets and
  decodeArg0Address() helper that pulls the first calldata argument.
  - Direct market calls (c.market === true) → c.evm
  - Market-arg-0 selectors (buy/sell PT/YT/LP) → decoded arg-0
  - SY-arg-0 selectors (zapHbarToSy, unzapSyToHbar) → null (action
    isn't market-scoped; user is at the SY layer)
  - Everything else → null

#10 — Indexer tick() re-entry guard (MEDIUM)
  setInterval(tick, INTERVAL_MS) is fire-and-forget. If one tick takes
  longer than INTERVAL_MS, the next tick was racing knownKeys() against
  in-flight inserts.
  FIX: top-level isTicking flag; new tick logs "tick_skip_overlap"
  and returns early if a previous tick is still running. Catch-up
  happens naturally on the next interval via the cursor.

#11 — Indexer cursor-based pagination (MEDIUM)
  Was `?order=desc&limit=50` per contract per tick. On burst traffic
  >50 tx in one INTERVAL_MS, older rows would silently fall off the
  page and never get indexed.
  FIX: first poll uses desc + limit (priming). Subsequent polls use
  `timestamp=gt:<lastSeen>&order=asc&limit=N`. Cursor lives in an
  in-memory Map keyed by contract EVM, advances on every poll regardless
  of insert outcome. On Railway restart, the Supabase dedup key
  (chain_id, tx_hash, event_type, address) prevents any double-insert
  during the priming phase.

#4 — ActionRouterV3.sol (V3 suffix) — DEFERRED, not a regression
  Predates the 2026-05-27 "no version markers" rule. File is referenced
  by FissionMegaZap (interface IActionRouterV3) and FissionUnzap (uses
  ROUTER), both deployed legacy contracts still in the indexer watchlist
  because users with old positions can still redeem. Renaming would
  cascade through 4+ files. Acceptable to leave: rule is 2026-05-27
  *onward* and these are pre-rebuild legacy. Will rename only if a
  legacy redeploy happens.

#5 — Slither baseline re-run vs Periphery v3 + SY v2 bytecode (MEDIUM)
  See PASS-3 ITEM #5 below for outcome.

#6 — isProtectedToken(SY) gap — FALSE POSITIVE
  Audit agent reported `Periphery.isProtectedToken(0xA0289A) = false`.
  But 0xA0289A is the SY adapter *contract*, not a token — protection
  is irrelevant (rescueToken's IERC20.safeTransfer would fail on a
  non-token address regardless). The actual SY share token that
  holders own is 0xA0289B and IS protected (verified on-chain via
  cast call). YT (0xa028aB) IS unprotected and represents a narrow
  owner-only front-run vector during transient periphery YT custody
  in sellYtForSy. Fix would require a Periphery v4 cascade (no
  addProtectedToken setter exists in v3). Documented as backlog;
  deferred since on-chain risk window is small + owner-only.

#7 — records.txt update — this entry.

#8 — ESLint v9 migration
  ESLint 9.17.0 was installed but no flat config existed → `npx eslint`
  errored with "couldn't find an eslint.config.(js|mjs|cjs) file". Lint
  has been silently non-functional in CI/local dev.
  FIX: frontend/eslint.config.mjs created using @eslint/eslintrc
  FlatCompat to extend next/core-web-vitals + next/typescript, with
  proper ignores for .next/playwright-report/test-results/node_modules.
  Lint runs cleanly now and surfaces 37 pre-existing issues (33 errors,
  4 warnings — all no-unused-vars + 1 explicit-any). Not blocking; lint
  debt to be paid down separately, not part of this fix.

#9 — auth_nonces cleanup primitives (LOW)
  Same migration file as #1. Adds partial index
  (address) WHERE consumed_at IS NULL for fast lookup; one-time DELETE
  of stale rows. Periodic cleanup left to future /api/auth/cleanup
  Vercel cron (gated by CRON_SECRET, route not yet built — backlog).

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
MIGRATION APPLIED 2026-05-27 22:30 IST (16:00 UTC)
═══════════════════════════════════════════════════════════════════════════════

supabase/migrations/20260527230000_lock_users_rls_and_nonces_cleanup.sql
applied via Supabase Management API (POST /v1/projects/<ref>/database/query)
using user-supplied Personal Access Token (token NOT logged here).

Verified end-to-end:
  - pg_policies: users_select_anyone DROPPED, users_select_self CREATED
    (SELECT, authenticated only, USING (lower(address) = public.jwt_address()))
  - information_schema.role_table_grants: anon SELECT on public.users REVOKED
    (grantee=anon → empty result)
  - public.auth_nonces_active_idx CREATED (partial: WHERE consumed_at IS NULL)
  - public.auth_nonces: 115 → 0 rows (all consumed; 1-day TTL deletion)
  - End-to-end: anon REST `GET /rest/v1/users?select=address,twitter_handle`
    returns HTTP 401-equiv: 42501 "permission denied for table users"
    (was leaking 8 rows pre-lockdown). PII closed.

Backlog still open: schedule periodic auth_nonces cleanup
(Vercel cron route `/api/auth/cleanup` gated by CRON_SECRET, not yet built).

═══════════════════════════════════════════════════════════════════════════════
PASS-4 (whole-project scope, third "are u sure" probe)
═══════════════════════════════════════════════════════════════════════════════

Stale-closure exhaustive-deps warnings (3) — REAL but practically inert:
  - SellPtForm.tsx:194 `runApprove` deps had `parsedPt` (unused in body)
    → removed
  - SellYtForm.tsx:271 `runSell` deps missed `detail.sy` (used at line 91
    in `args: [detail.sy, ...]`) → added
  - sy/page.tsx:246 unzap callback deps missed `detail.sy` (used at line 58
    in `args: [detail.sy, ...]`) → added
  In-practice impact zero because the form components remount on URL
  change (route param `[address]`), so detail.sy reference is fresh per
  mount. But the rule was right; closures should always pin their deps.
  Local `npm run build` passes, lint = 0 errors / 1 warning (the
  layout.tsx Google-Fonts <link> tag, perf-only).

Other Railway services in the user's workspace (discovered, NOT new):
  - `fission-indexer` (project: fission-indexer) — the activity-log poller
  - `fission-cache-cron` (project: fission-cache-cron) — separate
    Railway worker that POSTs /api/markets/refresh every 60s with
    CRON_SECRET. Verified live: 200 OK every minute, refreshes 6 markets
    (latest seen 16:54 UTC).
  These are two different Railway *projects* (not services within one
  project) so `railway status` inside `cron-indexer/` only sees the
  fission-indexer service. Worth documenting because future investigation
  by anyone (myself included) will look at one project and conclude the
  cache cron is missing — it isn't.

Dead code in repo (legacy, kept for source-history reasons):
  - `keeper/` — "Off-chain rate poster for Fission SY adapters. Reads
    source-of-truth rates from Stader / SaucerSwap V1 / Bonzo and posts
    to the on-chain SY contracts with bps + interval gating." Written
    for the old SY architecture that needed off-chain rate oracles.
    The current SY (SaucerSwapLPYieldSource v2) derives rate from LP
    token value on-chain — no off-chain posting needed. NOT deployed to
    any Railway service. Safe to delete on next cleanup pass; left in
    tree because deletion is irreversible and there's no urgency.

CI status: NOT enabled
  `.github/workflows/` does not exist. `.ci-staging/workflows/` contains
  contracts.yml + frontend.yml but they've never been activated. So
  `gh run list` returns empty — no CI to fail, but also no automated
  verification on push. Lint + typecheck + build now ALL pass locally;
  copying the staged workflows to .github/workflows/ would enable a
  proper CI gate. Deferred — user-call to activate (visible-state change).

PAT exposure check:
  `git grep "sbp_d6e0"` → nothing
  `grep "sbp_d6e0" records.txt` → nothing
  `git log -S "sbp_d6e0"` → nothing
  PAT exists ONLY in this Claude Code conversation transcript. User
  should rotate at https://supabase.com/dashboard/account/tokens.

CI staged workflows — DO NOT ACTIVATE without rewriting first:
  - .ci-staging/workflows/frontend.yml uses pnpm + frontend/pnpm-lock.yaml
    but the project ships npm + package-lock.json → install step would
    fail immediately. Rewrite needed: actions/setup-node with npm cache,
    `npm ci`, then npm run typecheck/lint/build.
    Also: `next lint` is deprecated in Next 16; should switch to
    `npx eslint src` (config is already at frontend/eslint.config.mjs).
  - .ci-staging/workflows/contracts.yml references
    `.github/slither.config.json` (does not exist) and runs
    `forge fmt --check` which currently has diffs in src/sy/* and likely
    other files. Either fix-and-commit a forge-fmt pass OR drop the
    `forge fmt --check` step.
  Net: copying these to .github/workflows/ as-is would mean every push
  goes red. Defer activation until rewrites are made.

═══════════════════════════════════════════════════════════════════════════════
ORPHAN-MARKET DRAIN 2026-05-27 22:50 IST
═══════════════════════════════════════════════════════════════════════════════

Initial scope estimate was ~$8 — wrong by 200x. Actual on-chain LP holdings
across the 3 orphan markets totaled ~$1,682 in SY-share face value. User
confirmed "drain SY now, hold PT for expiry" so the AMM was not touched.

Drained 3 orphans via operator-keyed market.removeLiquidity + sy.redeemLiquidity:
  - Market 0x3aCDD09b (SY=0x...a02585 OLD)  LP=69,892,233 → 0
  - Market 0x556938Ac (SY=0x...a02585 OLD)  LP=1,456,754,373 → 0
  - Market 0x15DeA525 (SY=0x...A0289A NEW)  LP=145,913,300 → 0

Key script-logic correction during dry-run: original script read operator's
TOTAL shareToken balance after removeLiquidity and would have passed that
to sy.redeemLiquidity — which would have ALSO drained the operator's
intentional canonical-market SY position (1.3B shares). Fixed to snapshot
balance pre-removeLiquidity and redeem only the delta.

Recovered now (operator wallet, post-drain): 49.27 USDC + 578.06 WHBAR
  ≈ $78 USD-equiv (HBAR≈$0.05). Lower than the $1,682 SY-face-value because
  the V3 NPM positions backing the orphan SY adapters are partially
  depleted relative to their share-supply (cascade iterations seeded with
  partial liquidity). The drain extracted everything extractable now.

PT held by operator (await 2026-08-25 expiry, then redeemAfterExpiry):
  - 0x...A02589 (m1):    70,039,373 PT
  - 0x...a0259F (m2): 1,447,576,920 PT
  - 0x...A0289d (m3): 1,459,142,007 PT
  Total: 2,976,758,300 PT. Face value at expiry: each PT redeems 1:1 for SY
  which unwinds via the V3 NFT. Estimated max ~$1,563 if NPM positions still
  contain proportional underlying — real recovery may be less.

Script: scripts/drain-orphans.mjs (driven by NEW_DEPLOYER_KEY).
6 contract-execute txs total: 3× removeLiquidity + 3× redeemLiquidity.
All SUCCESS per receipts.

Backlog: post-2026-08-25 second-leg drain
  Around 2026-08-25 (expiries: 04:14 UTC / 04:33 UTC / 19:30 UTC), call
  market.redeemAfterExpiry for each of the 3 PT holdings → operator gets
  SY shares → sy.redeemLiquidity → operator gets USDC + WHBAR. Add a
  reminder.

═══════════════════════════════════════════════════════════════════════════════
WALLET UNWIND TO HBAR 2026-05-28 (~04:18 IST)
═══════════════════════════════════════════════════════════════════════════════

Per user "swap WHBAR + USDC back to HBAR", consolidated operator wallet to
native HBAR only.

Steps:
  1. USDC.approve(V2_ROUTER, int64.max)              ✓
  2. SaucerSwapV2.exactInputSingle(USDC → WHBAR,     ✓
        fee=1500 0.15% pool 0xC5B707…0e7B11d,
        amountIn=49,272,145 raw, ≈$49.27)
     → received ~574 WHBAR (~0.5% pool fee + slippage)
  3. WHBAR_TOKEN.approve(WHBAR_CONTRACT, int64.max)  ✓
        (Hedera WHBAR wrapper uses transferFrom under the hood — HTS
         allowance required, unlike WETH9 which just decrements balanceOf.
         First withdraw attempt reverted with TOKEN_NOT_ASSOCIATED_TO_ACCOUNT
         /Safe token transfer failed! — this approve fixed it.)
  4. WHBAR_CONTRACT.withdraw(1153.32 WHBAR)          ✓
     → received native HBAR

Result:
  Before:  115.00 HBAR + 578.06 WHBAR + 49.27 USDC
  After:   1,266.64 HBAR + 0 WHBAR + 0 USDC
  Net HBAR gained: 1,152.53 HBAR ≈ $98.61 at $0.0855/HBAR

Script: scripts/unwind-to-hbar.mjs (driven by NEW_DEPLOYER_KEY).

═══════════════════════════════════════════════════════════════════════════════
CANONICAL MARKET LP TOP-UP 2026-05-28 (~04:30 IST)
═══════════════════════════════════════════════════════════════════════════════

User: "add 1,000 HBAR to the canonical market." Pool was tiny (~$50 backing,
brutal slippage for trades >$5). Goal: deepen for early users.

Path:
  1. Periphery.setMaxTradeBps(2000)  // raise X-3 cap 5% → 20%      ✓
  2. Periphery.zapHbarToSy(canonical, op, deadline) {value: 1000 HBAR}  ✓
     → operator received 1,453,000,000 raw SY shares (1,453 SY)
  ---- buySyForLp attempt would have failed: AMM swap portion (726 SY)
       exceeds totalPt (581) of pre-add pool. Switched to manual path: ----
  3. market.split(726.5 SY) → 726.5 PT + 726.5 YT                  ✓
  4. shareToken.approve(market, int64.max)                          ✓
  5. PT.approve(market, int64.max)                                  ✓
  6. market.addLiquidity(726.5 SY, 726.5 PT, 0, operator)           ✓
  7. Periphery.setMaxTradeBps(500)  // restore default              ✓

Result (canonical market):
  totalSy:  590.81 → 1,317.31  (+726.5,  +123%)
  totalPt:  581.56 → 1,296.68  (+715.1,  +123%)
  LP supply: 586.60 → 1,307.92  (+721.3,  +123%)
  Operator's LP: 586.60 → 1,307.92 (still 100% owner)

Pool size roughly 2.23x. Now supports ~$10-20 trades smoothly; tracks
total backing of ~$110-130 vs prior ~$50.

Operator residual after:
  HBAR:        264 → ~245 (gas)
  SY-shares:   2,761 → 1,308  (used 1,453 of the 1,453 zapped; original
                              1,308 loose still held for flexibility)
  PT (loose):  0.08 → 11.46  (curve refund from addLiquidity)
  YT (loose):  581.64 → 1,308.14  (PT/YT pair from split)
  LP:          586.60 → 1,307.92

Scripts: scripts/add-canonical-lp.mjs + scripts/finish-canonical-lp.mjs.
The first attempted buySyForLp but reverted (cap), zapHbarToSy still
succeeded so the second script picked up from there with the manual
split+addLiquidity path.

Pipeline: post-push watchdog will verify Vercel build green (per the
"deploy a local agent to verify after push" rule).

═══════════════════════════════════════════════════════════════════════════════
SESSION REVOCATION HARDENING 2026-05-28
═══════════════════════════════════════════════════════════════════════════════

Bug discovered during the post-RLS auth E2E test: sessions were stateless
HS256 JWTs with no server-side revocation. Logout cleared the browser cookie
but a captured cookie remained cryptographically valid for the full 7-day TTL
— /api/auth/me with a post-logout cookie still returned 200.

Fix (commit 5c23a74):
  - New public.sessions table (jti uuid pk, address, issued_at, expires_at,
    revoked_at, user_agent_summary). RLS service-role-only.
  - 3 indexes: jti partial (revoked_at IS NULL), address partial, expires.
  - session.ts: signSession() generates UUID jti, INSERTs row before
    issuing the cookie. verifySession() additionally queries the
    sessions table — rejects if missing/revoked/expired/address-mismatch.
  - /api/auth/logout extracts current jti, sets revoked_at, then clears
    the cookie.
  - Pre-jti tokens (older sessions) are now rejected — forces one re-login
    for any active user. Only operator had a session, impact = one re-login.

End-to-end verification:
  verify status            : 200
  /me before logout        : 200
  /me AFTER logout (same cookie): 401  ← critical, was 200 pre-fix
  Supabase row             : revoked_at populated immediately after logout

Cost: +1 Supabase round-trip per authenticated request (~5-10ms).

Migration: supabase/migrations/20260528000000_add_sessions_revocation.sql
applied via Management API. Schema + indexes + RLS + grants verified.

═══════════════════════════════════════════════════════════════════════════════
DOC CLEANUP 2026-05-28 (per user: replace stale contract refs everywhere)
═══════════════════════════════════════════════════════════════════════════════

  - records.txt: prepended "CURRENT STATE" header so canonical addresses
    are visible without parsing the whole forensic log.
  - contracts.txt: full rewrite. Was describing the 2026-05-22 Ed25519
    redeploy as "current"; now describes the 2026-05-27 + 2026-05-28
    canonical set. Abandoned set explicitly listed.
  - frontend/src/app/whitepaper/page.tsx: "Nine contracts… FissionZap +
    MegaZap + FissionUnzap + ActionRouter v3" → "Six contracts…
    FissionPeriphery (single consolidated entry point)". User-facing
    whitepaper now describes the actual deployed shape.
  - docs/ARCHITECTURE.md: ASCII architecture diagram updated. Replaced
    the ActionRouter UUPS box with the FissionPeriphery v3 box; replaced
    the "SY_HBARX / SY_BonzoUSDC" speculative SY adapter list with the
    actually-deployed SaucerSwapLPYieldSource v2.
  - README.md: already current per earlier work (2026-05-27 commit
    6771f0c). No edits this pass.

═══════════════════════════════════════════════════════════════════════════════
 SESSION SUMMARY 2026-05-27 → 2026-05-28 (consolidated)
═══════════════════════════════════════════════════════════════════════════════

Commits shipped this session (in order):
  4e84970  fix(WalletGate): React #310 hooks-rule violation
  42e2306  audit pass-3 — indexer fixes + RLS migration + lint + slither refresh
  b0d4ef2  fix(frontend): unblock Vercel build after ESLint v9 migration
  7f3a95d  records: log applied supabase RLS lockdown migration
  4bccdf1  fix(forms): pin detail.sy in 2 stale-closure deps + remove unused parsedPt
  f2db7ad  records: document why staged CI workflows aren't activated yet
  a4f9e1d  scripts: drain-orphans.mjs + log 3-market drain outcomes
  538962b  scripts: unwind-to-hbar.mjs — consolidate WHBAR+USDC back to native HBAR
  37cfea5  scripts: add-canonical-lp.mjs + finish-canonical-lp.mjs — top up canonical
  5c23a74  auth: server-side session revocation via public.sessions + jti claim
  afefc7c  docs: replace stale contract refs with current canonical set
  fbc4999  records: fix Factory address line in CURRENT STATE header

What changed at the protocol/data layer:

A. Smart contracts — UNCHANGED (no on-chain redeploys this session). The
   canonical set from 2026-05-27 morning rebuild is still live:
   Factory 0x799549F…, Periphery v3 0x…A02731, Lens 0xa1aAfc8…,
   SY adapter 0x…A0289A, Market 0xfD33CCB….

B. On-chain operator actions:
   1. Drained 3 orphan markets (0x3aCDD09b, 0x556938Ac, 0x15DeA525) of all
      operator LP. Recovered ~$78 USDC+WHBAR immediately. PT held for
      Aug-25 redeemAfterExpiry second leg.
   2. Unwound operator wallet to pure HBAR: USDC → WHBAR (SaucerSwap V2)
      → native HBAR via WHBAR.withdraw. Net +1,152 HBAR to wallet.
   3. Topped up canonical market LP by 1,000 HBAR: zapHbarToSy then
      market.split + market.addLiquidity (buySyForLp couldn't run because
      pool was too small for the AMM swap step). Pool grew 2.23x —
      totalSy/totalPt/lpSupply all +123%.

C. Frontend / database hardenings:
   - Fixed React #310 in WalletGate (useState after early return).
   - Supabase RLS lockdown on public.users (was anon-readable, now self-only).
   - Indexer: long-zero → ECDSA alias resolution; market_address from
     calldata arg-0 (was dead ternary); tick re-entry guard; cursor
     pagination via timestamp=gt:.
   - ESLint v9 migrated to flat config; pre-existing lint debt cleaned.
   - Server-side session revocation: new public.sessions table tracking
     every JWT by uuid `jti`. /api/auth/logout now revokes before
     clearing cookie — captured cookies can't outlive logout. Verified
     end-to-end: /me with same cookie after logout returns 401 (was 200).
   - 3 stale-closure useCallback deps fixed (SellPtForm, SellYtForm,
     sy/page.tsx).

D. Docs / ops:
   - records.txt prepended CURRENT STATE header (this header was added
     2026-05-28).
   - contracts.txt fully rewritten — was claiming pre-rebuild set as
     "current"; now describes the 2026-05-27/28 canonical.
   - frontend whitepaper page: "Nine contracts … FissionZap + MegaZap +
     FissionUnzap + ActionRouter v3" → "Six contracts … FissionPeriphery
     (single consolidated entry point)".
   - docs/ARCHITECTURE.md ASCII diagram: ActionRouter UUPS box →
     FissionPeriphery v3 box with the 6 deterministic 2-tx flows.

E. Wallet state (operator 0.0.10495279 / 0xa7e1…11a7) at end of session:
   HBAR        : ~262
   USDC        : 0
   WHBAR       : 0
   LP (canonical): 1,307.92  (100% of pool supply 1,307.93)
   SY-shares (loose): 1,308.46  (current SY adapter shareToken 0x…A0289B)
   YT (canonical): 1,308.14
   PT (canonical): 11.46  (dust from addLiquidity refund)
   PT (orphans): 70.04 + 1,447.58 + 1,459.14 = 2,976.76 face value,
                 redeemable at 2026-08-25 expiry

F. Pending / deferred (your strategic calls):
   - Funding gap: another 2-5k HBAR top-up would deepen canonical to
     where $50-200 trades feel smooth.
   - Timelock ownership handoff (FissionPeriphery owner = operator EOA;
     should transfer to 2-of-2 ThresholdKey).
   - 2026-08-25 calendar reminder: redeemAfterExpiry on 3 orphan PTs +
     canonical LP exit decision.
   - PAT rotation at https://supabase.com/dashboard/account/tokens —
     the sbp_d6e0… token was shared in chat.
   - CI workflows in .ci-staging/ — need rewrite (pnpm→npm + drop
     forge-fmt + add slither config) before activation.

G. Subagent watchdogs deployed this session (some, not all — user
   explicitly flagged the gap mid-session):
   - 4e84970 ✓  42e2306 ✓  b0d4ef2 ✓  37cfea5 ✓  5c23a74 ✓  fbc4999 ✓
   - 7f3a95d, 4bccdf1, f2db7ad, a4f9e1d, 538962b, afefc7c missed.
   Memory feedback updated 2026-05-28 with stronger no-skip language.

H. Bugs found mid-execution + how they got caught:
   - React #310 in WalletGate — caught by opera-browser-cli console probe,
     not by build/lint (the code compiled fine).
   - Supabase users PII leak — caught by anon REST query exposing 8 rows.
   - Indexer's market_address dead ternary (c.market?c.evm:c.evm) —
     caught by code review during sub-agent audit.
   - ESLint v9 silently broken (no config) — caught when migration
     surfaced 33 pre-existing errors that broke build.
   - Stateless-session no-revoke — caught by E2E test where /me after
     logout returned 200.
   - Pre-flight orphan-drain script would have redeemed operator's
     intentional canonical SY position too — caught in dry-run.
   - WHBAR.withdraw needs HTS allowance from operator (unlike WETH9) —
     caught when first withdraw attempt reverted TOKEN_NOT_ASSOCIATED.
   - buySyForLp swap portion exceeds totalPt on tiny pool — caught when
     1,453 SY zap was too big for the AMM, switched to split+addLiquidity.

   Pattern: every bug found was discovered by ACTUALLY RUNNING the
   thing end-to-end, not by reasoning at it. Browser-probe, on-chain
   tx, REST query, dry-run — these are the safety net.

═══════════════════════════════════════════════════════════════════════════════
CANONICAL MARKET REDEPLOY: 8% APY 2026-05-28
═══════════════════════════════════════════════════════════════════════════════

The previous canonical (0xfd33ccb…) had anchor=1.2e18 set at deploy
(20% initial APY — already misaligned with the observed ~9.7%
SaucerSwap LP yield). Subsequent smoke tests + a botched rebalance
attempt drove lastLnImpliedRate to 0.879 = 141% APY. User flagged the
display as broken (rightfully — Pendle-style markets should track
underlying yield within reason).

Two-step redeploy:

Step 1: created 0x432E552A… with anchor=1.08e18, expecting 8% APY.
        Got 37% APY instead because Pendle V2 treats `anchor` as a
        per-year-period rate factor, NOT annualized APY. The contract
        annualizes it via (yearSec / timeToExpiry). With 89 days left,
        1.08 → 1.08^(365/89) ≈ 37%.

Step 2: created 0x781382351c9Ed32df3110B8d805D3C8C3dBFe046 with
        anchor=1.018934e18 (computed as exp(ln(1.08) × timeToExpiry / yearSec)).
        → Initial lastLnImpliedRate = 0.077 = 8.00% APY ✓
        → totalSy = totalPt = 208,533,424 (operator funded 100% LP)

Math for the future: to seed a market at APY A with `t` seconds to expiry,
        anchor = exp(ln(1+A) × t / yearSec)
                                            ; for 8% APY at 89d → 1.019e18
                                            ; for 8% APY at 365d → 1.08e18

Operator final position on new market:
  - 100% LP ownership
  - 208M YT (operator-only yield exposure)
  - Old market PT/YT still held — claim at Aug-25 expiry via redeemAfterExpiry
  - Failed-attempt PT/YT still held — same Aug-25 expiry

Scripts: redeploy-canonical-8pct.mjs (failed-37%-attempt) +
         redeploy-canonical-8pct-precise.mjs (succeeded).

Total HBAR cost across both attempts: ~140 HBAR (~$12) — pure Hedera
HTS-create tax for two market deploys (4 HTS tokens each).

═══════════════════════════════════════════════════════════════════════════════
SOURCE IMPROVEMENTS — POST-AUDIT FINDINGS FROM 2026-05-28
═══════════════════════════════════════════════════════════════════════════════

After the line-by-line contract review post-canonical-redeploy, three
source-level improvements landed (will deploy on next cascade — live
Periphery is unchanged because it's immutable):

#1 — YT now included in isProtectedToken on _registerMarket
    File: contracts/src/periphery/FissionPeriphery.sol
    Change: `isProtectedToken[yt] = true` added alongside shareToken/pt/lp.
    MarketRegistered event already includes `yt`; now matches state.
    Rationale: belt-and-suspenders. Current flows route YT directly to
    `receiver` so YT is never custodied by Periphery, but if a future flow
    buffers YT mid-tx the rescue gate would have left it exposed. Cost:
    one SSTORE per market init (~5k gas).

    LIVE-CHAIN STATUS: Periphery 0x...A02731 still has YT unprotected.
    Not currently exploitable. Will be protected when this updated source
    is deployed in a future Periphery cascade.

#2 — NatSpec warning on SaucerSwapLPYieldSource.redeemLiquidity
    File: contracts/src/sy/SaucerSwapLPYieldSource.sol
    Change: doc block flagging the `amount0Min/amount1Min = 0` risk for
    direct callers who bypass the Periphery. If the V3 pool position is
    drained (other LPs pulled out, emergency rescue, etc.) NPM's
    decreaseLiquidity can return (0, 0); shares get burned for nothing.
    FissionPeriphery.unzapSyToHbar already enforces minHbarOut downstream
    so periphery flows are safe; direct integrators are warned.

#3 — Added refreshMarketApprovals(market) admin escape hatch
    File: contracts/src/periphery/FissionPeriphery.sol
    Change: new external onlyOwner function to re-prime allowances on
    an already-registered market. `_registerMarket` is silently
    idempotent (line 248 `if (marketRegistered[market]) return`) which
    means a re-call doesn't refresh approvals. The new function lifts
    that limitation without violating the idempotent registration
    semantic. Does not toggle marketRegistered or emit the registration
    event.

#4 — Operator HBAR LOW
    Operator balance: 68 HBAR (~$5.84). After today's deploys + drains
    + rebalances. Insufficient to deploy a new Periphery (~100 HBAR
    minimum for the contract + HTS associations on init). Top up before
    any future cascade.

Tests: forge test → 322 passed, 0 failed, 3 skipped. Same as pre-edit.

═══════════════════════════════════════════════════════════════════════════════
 AMM FEE REDIRECT — COMMIT, FORENSIC AUDIT + LIVE EXERCISE  2026-05-29
═══════════════════════════════════════════════════════════════════════════════

REPO RELOCATED: the entire ~/Desktop/Projects tree was moved to
~/Documents/Projects to escape a recurring macOS TCC Full-Disk-Access block on
the Desktop folder (every Claude auto-update re-staled the grant; the
Documents-folder grant is intact). Repo is now
~/Documents/Projects/fission-protocol-hedera — read every ~/Desktop/... path
above as ~/Documents/... .

COMMITS PUSHED TO main:
  d1dc6e4  contracts+frontend: AMM fee redirect cascade (49.5/49.5/1)
           — the live market 0xfEcf was built from previously-UNCOMMITTED
             source; this commit reconciles repo <-> deployed bytecode.
  8c21457  fix(display-layer): DL-1 / DL-2 / DL-3 / FE-01 + dead-helper

CANONICAL LIVE SET (chain 295; on-chain unchanged this session):
  Factory   0xc6e077bfd0b2dfc2bf5694219242ef11aa46086d  (0.0.10500835)
  Market    0xfEcfC0Bb57dD668fF37F2A232b208584E5FeAE53  (0.0.10500837) exp 2026-08-25
  PT/YT/LP  0x..A03aE6 / 0x..A03aE7 / 0x..A03aE8
  StdDeployer 0x8d56747f.. (0.0.10500818)  RwdDeployer 0x4454adc3.. (0.0.10500834)
  Periphery 0x..A02731  Lens 0xa1aA..1969  SY 0x..A0289A (share 0x..A0289B) [reused]
  Prior market 0x781382.. archived to previousMarkets[] / indexer old_markets.

FORENSIC AUDIT (read-only, 6-dimension parallel + adversarial verification):
  VERDICT: on-chain CLEAN — no Critical/High logic bugs. Verified: AMM fee
  split (bps sum 10000, dust->YT), PT denominator = pt.totalSupply() (not
  totalPt), all 8 settlement-hook sites settle-before / mark-after
  (redeemAfterExpiry settles PT-AMM pre-burn), claimAmmRewards consolidation,
  shareToken cache, conservation invariant (sum(claims) <= collected fees).
  ABI<->bytecode consistent across all 9 addresses. RLS default-deny, auth
  gating, security headers correct. Live implied APY 9.26% (lnImpliedRate
  0.08857) == displayed; legit price discovery, NOT the prior 141% pathology.

  Display-layer defects (zero funds at risk) — all fixed in 8c21457:
    DL-1 (MED) /api/markets served superseded market 0x781382 un-archived
         beside 0xfEcf (refresh route omitted is_archived). FIX: is_archived in
         upsert + prior-factory archive sweep (gated rows>0). Self-healed live:
         /api/markets now returns exactly one active market.
    DL-2 (MED) cron-indexer bundled deployments/295.json still pinned the old
         factory/market -> the 6 direct-to-market actions on 0xfEcf were not
         indexed (periphery-routed buy/sell were fine). FIX: bundled JSON ->
         new factory/market; old kept under abandoned.old_factories/old_markets.
         fission-indexer redeployed via `railway up` (a restart alone is
         insufficient — the watchlist is baked into the image). Verified:
         startup log shows the new watchlist (contracts=15); first tick
         ingested the new-market direct actions, 0 errors.
    DL-3 (LOW) loadContractsFromDeployments now normalizes the canonical
         repo-root 295.json shape so the PARENT_REPO fallback cannot yield an
         empty watchlist.
    FE-01 (LOW) Footer.tsx Market/Factory HashScan links -> canonical.
    dead-helper (LOW) periphery2tx.ts buySyForLp was 6-arg vs the 7-arg
         contract (dead code; live ProvideLpForm builds the correct call) —
         fixed to 7-arg.
  Adversarial verify OVERTURNED the watchdog's "indexer hasn't ingested the new
  market" claim: the new market was the freshest /api/markets row; the real bug
  was the missing archival flag on the OLD one (DL-1).

LIVE ON-CHAIN EXERCISE (Phase B, operator key, real funds, ~14.5 HBAR):
  smoke-all-periphery.mjs 12/12 SUCCESS on 0xfEcf (zap, buy/sell PT, buy/sell
  YT, sell LP, unzap, approvals, setOperator).
  scripts/audit-phaseb-claim-lp.mjs (the headline-feature exerciser):
    - claimAmmRewards(op) returned (pt=0, yt=18863); operator SY-share delta
      = +18863 raw — EXACT match to the value independently computed from the
      on-chain indices. PT-side 0 (operator holds 0 external PT; in-pool PT
      forfeit by design). Second claim returned (0,0) — no-op, no double-spend.
    - The 8 swaps moved ptAmmRewardIndex 5.45e13->1.09e14 and ytAmmRewardIndex
      likewise — fee redirect accrues on live trades.
    - buySyForLp (7-arg add-LP, not in the smoke): lpOut 1,487,729; LP delta matched.
  Operator HBAR 282.31 -> 267.84.

VERIFICATION: read-only watchdog agents confirmed both Vercel deploys READY
  (SHA match), live 200, footer fix live, DL-1 self-heal across 8 polls; DL-2
  indexer redeploy verified directly via railway logs.

NOTES:
  - DL-1a manual Supabase UPDATE was NOT needed: Fission Supabase
    (atjsjwebftwbaellnuuy) is not in the MCP (only pengucrush is), but the
    deployed refresh-route sweep self-archives 0x781382 on its own cron tick.
  - Railway: CLI authed (penguinpecker1@gmail.com); fission-indexer deploys via
    `railway up` from cron-indexer/ (Nixpacks, node index.js). Watchlist = the
    bundled cron-indexer/deployments/295.json (edit + `railway up` to change).

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
 UUPS-PROXY + FREEZE-PT REBUILD -> LIVE, SWEPT, AUDITED, REMEDIATED  2026-05-29/30
═══════════════════════════════════════════════════════════════════════════════

WHY: the AMM-fee-redirect market (0xfEcf) sent 49.5% of swap fees to "PT holders"
but accrued against the HTS facade balanceOf, which REVERTS for Ed25519
(HashPack-default) long-zero holders -> their PT-side fee was forfeit. Also wanted
the "brain" contracts upgradeable so minor fixes don't need a full market redeploy.

THE FIX (FissionRewardsMarket): PT is now freeze-by-default with a contract-tracked
_ptBal mirror (cloned from the YT pattern) + Ed25519-safe ptBalanceOf; PT-side fee
accrual reads _ptBal, not the reverting facade. PT is non-transferable externally;
direct sell-PT / add-liquidity route through operator-gated swapExactPtForSyFor /
addLiquidityFor (user does setOperator(periphery,true) once); periphery is
freeze-exempt. Periphery / Lens / Factory are now UUPS proxies (ERC1967,
_disableInitializers, storage gap, _authorizeUpgrade -> UPGRADER_ROLE); cores
(Market, SY adapter, both MarketDeployers) stay IMMUTABLE. Factory swapped
ACDAR -> plain AccessControl (proxy-safe) with a guarded admin renounce.

NEW LIVE SET (chain 295; deployments/295.json is source of truth):
  Market (Rewards, immutable)  0x31742aFF65fcfed391cc6B4B9DA0271643e0Eec6 exp 2026-08-27
  Factory  (UUPS proxy)        0x..a03f94   (impl 0x..a03f92)
  Periphery(UUPS proxy)        0x..a03fAd   (impl 0x..A03FAB)
  Lens     (UUPS proxy)        0x..a03f9b   (impl 0x..a03F98)
  SY adapter (SaucerSwapLPYieldSource, immutable) 0x..a03f9d ; share 0x..A03f9E
  PT 0x..A03FA6 / YT 0x..a03fa7 / LP 0x..a03fa8   (all 18-dec; old gen also 18-dec)
  StdDeployer 0x..a03f8a ; RwdDeployer 0x..a03f8f
  Deployer/operator 0xA7e1..11a7 (0.0.10495279). Prior canonical 0xfEcf archived.

DEPLOY/SEED: deploy-rebuild-proxy.mjs (manual FileCreate+FileAppend, hex-TEXT for
  big bytecode, retry on TRANSACTION_EXPIRED). Seeded via seed-init-rebuild-market.mjs:
  zap 4000 HBAR -> ~6.03e9 SY -> split half -> initialize(syIn, ptIn, anchor=
  1019153009660895232 [8% annualized->per-period], lnFeeRoot=1e16, reserve=50).
  Pool: totalSy==totalPt ~3.016e9 each (~2000 HBAR/side); APY guard PASS 8.0001%.
  379 forge tests green; adversarial multi-agent security audit (contracts SOLID:
  solvency/conservation, freeze-mirror Ed25519-safe + conservative, UUPS genuine,
  fee split correct). Live real-funds smoke + on-chain battery (split/merge/add/
  remove/claim/unzap + slippage-revert negatives + Lens accuracy) all PASS.

GO-LIVE: commit 54d97de -> origin/main. Vercel (fission-protocol, penguinpeckers-
  projects) 5 prod env vars set to the new addresses (sensitive/write-only). Prod
  deploy Ready; live site serves ONLY the new market (old archived). Railway
  fission-indexer redeployed (`railway up`) with the new bundled 295.json watchlist.
  markets_cache repopulated (cache-only list; daily cron refresh).

STRANDED-LIQUIDITY DRAINS:
  (a) Post-rebuild stale markets (drain-stale-markets.mjs): ~$448 recovered via
      permissionless merge() (PT+YT 1:1 -> SY, no AMM/anchor-bug exposure) +
      removeLiquidity + redeemLiquidity (direct SY-adapter calls, simulated
      min-outs). Earlier "$53" estimate was wrong; merge unlocks ~everything now.
  (b) Old-deployer (0.0.10463169) pre-rebuild positions (sweep-old-deployer.mjs):
      ~$43 recovered (399M v1 SY shares + FIX-market 0x36ed8f LP + loose
      HBAR/USDC/WHBAR) -> consolidated to the current deployer. Old key derived
      from SEED_PHRASE via the standard Hedera path (toStandardECDSAsecp256k1-
      PrivateKey), verified == 0x32e8.. before any move; never persisted.
      Mkt0 (0xfa90) merge reverts (that old market's frozen-YT bug) -> $0.89 left;
      ~10B FIX PT + the YT are BRICKED (FIX holds only 119.7M SY vs 6.57B+ PT) =
      worth ~0. v1 SY adapter (0x..9fb089) NFT ~$766 total; only ~$37 was the old
      deployer's to claim.

WHBAR ALLOWANCE SWEEP (the ONE real loss, ~$174): right after drain (a), external
  account 0.0.995584 pulled 1,902 WHBAR from the current deployer via a STANDING
  WHBAR -> WHBAR-contract allowance (no HBAR returned). Remediated: unwrapped the
  remaining WHBAR -> HBAR + REVOKED that allowance. Then ALL 36 standing token
  allowances on the deployer were revoked (revoke-allowances.mjs; spender EVM
  resolved from the mirror since CREATE-deployed spenders != long-zero(id)). Wallet
  now carries ZERO standing approvals. LESSON: never leave near-max HTS allowances
  standing; check `allowances/tokens` periodically.

ACCOUNTING RECONCILIATION (wallet-ledger.mjs) — two scares traced + CLEARED:
  - the 224 USDC that appeared "missing" was SWAPPED to ~2,444 HBAR (self-initiated
    contractcall; value preserved, in-wallet) — NOT a loss (I over-alarmed; verify
    before claiming theft).
  - a -5,000 HBAR transfer went to cosigner B 0.0.10457309 (known co-signer).
  - "EXTERNAL" ledger flags were Hashio-relayed self gas, not theft.
  Net: the ONLY genuine loss all session was the ~$174 WHBAR above (contained).

FULL-STACK FORENSIC AUDIT (35 agents, 61 findings, 0 refuted; web2+web3):
  Contracts SOLID. Live FRONTEND was functionally broken for fresh users:
    W2-01 Buy PT sent the SY budget as the contract's minPtOut arg -> ~2% shortfall
      + inert slippage. W2-04 no setOperator GRANT UX -> Sell PT/YT revert for every
      fresh user (only the deployer had it set, masking it). W2-03 Claim/AMM-reward/
      redeem unreachable (dead "Claim All" link, no adapter op) while rewards accrue
      on-chain. W2-02/05 Remove-LP / Sell-PT missing SY-share approval + wrong unzap
      amount. W2-06/07/08 slippage floors decorative / ApprovalsCard wrong SY token.
    WEB2-IDOR-02 /api/activity readable for ANY address unauthenticated (service-role
      bypasses RLS; RLS is vestigial - custom HS256 cookie so jwt_address()='').
    IDX-01 indexer selector-map missing claimAmmRewards/merge -> claim events dropped;
      IDX-03 reverted txs stored as SUCCESS; watchlist key mismatch (7 legacy
      contracts un-indexed).
    LP-1 login origin rejected the vercel.app alias; LP-2 no CSP; LP-3 >2^53 int JSON
      rounding; WEB2-01/02/AUTH-01 Hedera-auth no domain/chain binding, no rate-limit,
      /api/auth/cleanup was a 404.

REMEDIATION (3 parallel agents, then verified): commits 4fa29fe (frontend trade/
  claim + API/auth/security + indexer) + a263cbd (CSP report-only -> ENFORCE).
  Frontend: minPtOut from Lens estimate; one-time "enable selling" setOperator step;
  SY-share approvals on the exits; Lens-derived min-outs; wired the dead Claim/
  Redeem/Claim-All buttons to real txs. API: /api/activity session-bound; origin
  fail-closed; Hedera host+chainId binding; CSP (verified ZERO violations via browser
  across all pages + the wallet-connect modal flow, THEN flipped to enforce); per-IP
  rate limits; real /api/auth/cleanup cron; constant-time secret checks; opaque
  errors; big-int string serialization; truthful RLS comments. Indexer: selector map
  regenerated from ABIs (+claimAmmRewards/merge/etc.); reverted txs skipped; watchlist
  aligned; dedup/quarantine. All build+typecheck green; deployed (Vercel + Railway);
  live-verified (header flipped, activity 401, pages 200, wallet modal works).

DB MIGRATION + DRIFT FIX: applied 20260529000000_drop_dead_lower_address_trigger
  (drops a dead/unattached fn) via the Supabase MANAGEMENT path (the linked CLI
  account is a limited member -> 403; a management token is required). FOUND
  migration-history DRIFT: 20260527230000 (users PII lockdown) + 20260528000000
  (sessions-revocation table) were applied to the live DB out-of-band but never
  recorded (history stopped at 20260525). Verified their effects were live,
  `supabase migration repair --status applied` recorded them, then `db push` applied
  only the new one. Remote now fully in sync (8/8). [The management token was
  supplied transiently this session + MUST BE ROTATED; never written to repo/records.]

STILL OPEN:
  - #11 multisig/timelock handoff (DEFERRED by user): operator EOA 0xA7e1..11a7 is
    still sole admin/upgrade-authority on all proxies. Target = Hedera 2-of-2
    ThresholdKey + Timelock; also move UPGRADER_ROLE. Literal last on-chain step.
  - ROTATE the Supabase personal access token (it was shared in chat this session).
  - W3 lows (next deploy/upgrade): two-step setUpgradeAuthority, setPeriphery guard,
    storage-layout CI guard, record factoryImpl/lensImpl in 295.json.
  - Residual dust: ~$0.22 PT redeemable at the Aug-25 expiry; ~$0.57 (6.255 HBAR)
    permanently stuck in old SY adapter 0x..a02585 (no sweep fn).

NEW SCRIPTS (scripts/): deploy-rebuild-proxy, seed-init-rebuild-market,
  smoke-test-live-market, drain-stale-markets, forensic-onchain, revoke-allowances,
  unwrap-whbar, liquidity-report, residual-scan, value-old-deployer, derive-old-key,
  sweep-old-deployer, wallet-ledger.

═══════════════════════════════════════════════════════════════════════════════
